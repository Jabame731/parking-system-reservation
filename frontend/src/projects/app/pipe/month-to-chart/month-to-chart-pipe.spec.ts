import { MonthToChartPipe } from './month-to-chart-pipe';

describe('MonthToChartPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthToChartPipe();
    expect(pipe).toBeTruthy();
  });
});
