import { ParkingSlotItemsPipe } from './parking-slot-items-pipe';

describe('ParkingSlotItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ParkingSlotItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
