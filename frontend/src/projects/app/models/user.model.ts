export interface User {
  id: string;
  firstName: string;
  lastName: string;
  address: string;
  contactNumber: string;
  email: string;
  image?: string;
  userRole?: string;
  updatedAt?: string;
  createdAt?: string;
}
